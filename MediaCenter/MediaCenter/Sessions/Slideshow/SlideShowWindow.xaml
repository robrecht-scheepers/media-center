<Window x:Class="MediaCenter.Sessions.Slideshow.SlideShowWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:interactivity="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:local="clr-namespace:MediaCenter.Sessions.Slideshow"
        xmlns:query="clr-namespace:MediaCenter.Sessions.Query"
        xmlns:media="clr-namespace:MediaCenter.Media"
        xmlns:controls="clr-namespace:MediaCenter.WPF.Controls"
        mc:Ignorable="d"
        Title="SlideShowWindow" Height="300" Width="300"
        WindowState="Maximized" WindowStyle="None"
        d:DataContext="{d:DesignInstance local:SlideShowViewModel}">
    <Window.Resources>
        <ResourceDictionary>
            <DataTemplate DataType="{x:Type media:ImageItemViewModel}">
                <Image Source="{Binding Path=MediaItem.Content}" Stretch="Uniform" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type media:VideoItemViewModel}">
                <controls:VideoPlayer VideoUri="{Binding Path=MediaItem.ContentUri}" Rotation="{Binding MediaItem.Rotation}" StartOnLoad="True" />
            </DataTemplate>
        </ResourceDictionary>
    </Window.Resources>
    
    <Window.InputBindings>
        <KeyBinding Key="Escape" Command="{Binding CloseCOmmand}"/>
    </Window.InputBindings>

    <interactivity:Interaction.Behaviors>
        <local:HideIdleMouseBehavior Interval="00:00:04"/>
    </interactivity:Interaction.Behaviors>

    <Grid Background="Black">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="4*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ContentControl Grid.Row="0" Grid.RowSpan="3" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Content="{Binding SelectedItemViewModel}"/>
        <controls:ImageButton Grid.Row="0" ImageSource="/Resources/Icons/cross-grey.png" Command="{Binding CloseCOmmand}" Width="32" Height="32" HorizontalAlignment="Right"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <controls:ImageButton ImageSource="/Resources/Icons/control-180.png" Command="{Binding SelectPreviousImageCommand}" Width="32" Height="32"/>
            <controls:ImageButton ImageSource="/Resources/Icons/film.png" Command="{Binding PauseResumeCommand}" Width="32" Height="32"/>
            <controls:ImageButton ImageSource="/Resources/Icons/control.png" Command="{Binding SelectNextImageCommand}" Width="32" Height="32"/>
        </StackPanel>
    </Grid>
</Window>
