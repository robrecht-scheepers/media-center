<UserControl x:Class="MediaCenter.Sessions.Query.QuerySessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:MediaCenter.Sessions.Query"
             xmlns:sessions="clr-namespace:MediaCenter.Sessions"
             xmlns:style="clr-namespace:MediaCenter.Styles"
             xmlns:interactivity="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:slideshow="clr-namespace:MediaCenter.Sessions.Slideshow"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance local:QuerySessionViewModel}"
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Sessions/Query/Filters/FilterDataTemplates.xaml"/>
                <ResourceDictionary Source="/Styles/Styles.xaml"/>
                <ResourceDictionary Source="/Media/MediaItemDataTemplates.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <interactivity:Interaction.Behaviors>
        <slideshow:OpenCloseWindowBehavior WindowType="slideshow:SlideShowWindow" Trigger="{Binding SlideShowActive}" WindowDataContext="{Binding Path=SlideShowViewModel}" />
    </interactivity:Interaction.Behaviors>
    
    <UserControl.InputBindings>
        <KeyBinding Key="Right" Modifiers="Control" Command="{Binding SelectNextImageCommand}"/>
        <KeyBinding Key="Left" Modifiers="Control" Command="{Binding SelectPreviousImageCommand}"/>
    </UserControl.InputBindings>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="4*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Column="0" Grid.Row="0" Orientation="Horizontal">
            <ComboBox ItemsSource="{Binding FilterNames}" SelectedItem="{Binding SelectedFilterName}" VerticalAlignment="Top"/>
            <Button Command="{Binding AddFilterCommand}" VerticalAlignment="Top">+</Button>
        </StackPanel>
        
        <ListView Grid.Column="0" Grid.Row="1" ItemsSource="{Binding Filters}" HorizontalContentAlignment="Stretch">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <ContentPresenter Grid.Column="0" Content="{Binding}"/>
                        <Button Grid.Column="1" Style="{StaticResource CrossIconButton}" 
                                CommandParameter="{Binding}" 
                                Command="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type UserControl}}, Path=DataContext.RemoveFilterCommand}"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Button Grid.Column="0" Grid.Row="2" Command="{Binding ExecuteQueryCommand}">Search</Button>
        
        <ListBox Grid.Column="1" Grid.Row="0" ItemsSource="{Binding QuerySession.QueryResult}" SelectedItem="{Binding Path=SelectedItem, Mode=TwoWay}"
                 ScrollViewer.HorizontalScrollBarVisibility="Auto">
            <interactivity:Interaction.Behaviors>
                <style:ScrollIntoViewBehavior/>
            </interactivity:Interaction.Behaviors>
                <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Image Source="{Binding Thumbnail}" Height="100" Width="100" Margin="5"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ListBox>

        <Grid Grid.Column="1" Grid.Row="1" Grid.RowSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <ContentControl Content="{Binding SelectedItem}" />
            <Button Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Center" Style="{StaticResource IconButton}" Command="{Binding SelectPreviousImageCommand}">
                <Image Source="/Resources/Icons/arrow-180.png" Stretch="None"/>
            </Button>
            <Button Grid.Column="0" HorizontalAlignment="Right" VerticalAlignment="Center" Style="{StaticResource IconButton}" Command="{Binding SelectNextImageCommand}">
                <Image Source="/Resources/Icons/arrow.png" Stretch="None"/>
            </Button>
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" Orientation="Horizontal">
                    <ToggleButton Style="{StaticResource IconToggleButton}" IsChecked="{Binding SelectedItem.Favorite, Mode=TwoWay}">
                        <Image Stretch="None">
                            <Image.Style>
                                <Style TargetType="Image">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding SelectedItem.Favorite}" Value="true">
                                            <Setter Property="Source" Value="/Resources/Icons/star.png"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding SelectedItem.Favorite}" Value="false">
                                            <Setter Property="Source" Value="/Resources/Icons/star-empty.png"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Image.Style>
                        </Image>
                    </ToggleButton>
                    <ToggleButton Style="{StaticResource IconToggleButton}" IsChecked="{Binding SelectedItem.Private, Mode=TwoWay}">
                        <Image Stretch="None">
                            <Image.Style>
                                <Style TargetType="Image">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding SelectedItem.Private}" Value="true">
                                            <Setter Property="Source" Value="/Resources/Icons/lock.png"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding SelectedItem.Private}" Value="false">
                                            <Setter Property="Source" Value="/Resources/Icons/lock_grey.png"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Image.Style>
                        </Image>
                    </ToggleButton>
                    <Button Style="{StaticResource IconButton}" Command="{Binding DeleteCurrentImageCommand}">
                        <Image Source="/Resources/Icons/cross-button.png" Stretch="None"/>
                    </Button>
                    <Button Style="{StaticResource IconButton}" Command="{Binding CopyTagsFromPreviousCommand}">
                        <Image Source="/Resources/Icons/arrow-repeat.png" Stretch="None"/>
                    </Button>
                </StackPanel>
                <sessions:TagsView Grid.Row="1" DataContext="{Binding TagsViewModel}"/>
                <Button Grid.Row="2" Command="{Binding StartSlideShowCommand}">Start Slideshow</Button>
            </Grid>
        </Grid>
        
        

        
        
    </Grid>
</UserControl>
