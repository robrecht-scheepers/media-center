<UserControl x:Class="MediaCenter.Sessions.Staging.StagingSessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MediaCenter.Sessions.Staging"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance local:StagingSessionViewModel}"
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../../Styles/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal"> 
              <Button Command="{Binding AddImagesCommand}" Width="100" Margin="5">Select images files</Button>
              <Button Command="{Binding AddDirectoryCommand}" Width="100" Margin="5">Select folder</Button>
              <Button Command="{Binding SaveToRepositoryCommand}" Width="100" Margin="5">Save</Button>
              <TextBlock Text="{Binding StagingSession.StatusMessage}" Margin="5"/>
        </StackPanel>
        <ListBox Grid.Row="1" ItemsSource="{Binding StagingSession.StagedItems}"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Vertical">
                        <Grid>
                            <Image Stretch="UniformToFill" Source="{Binding Thumbnail}" HorizontalAlignment="Center" Height="100" Width="100" Margin="5"/>
                            <Button HorizontalAlignment="Right" VerticalAlignment="Top" Style="{StaticResource IconButton}" 
                                    Command="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type UserControl}}, Path=DataContext.RemoveItemCommand}" 
                                    CommandParameter="{Binding}">
                                <Image Source="/Resources/Icons/cross-button.png" Width="16" Height="16"/>
                            </Button>
                        </Grid>
                        <TextBlock Text="{Binding Name}" Margin="5" />
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ListBox>

    </Grid>
</UserControl>
