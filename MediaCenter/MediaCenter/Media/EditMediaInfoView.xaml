<UserControl x:Class="MediaCenter.Media.EditMediaInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MediaCenter.Media"
             xmlns:tags="clr-namespace:MediaCenter.Sessions.Tags"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance local:EditMediaInfoViewModel}">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/Styles.xaml"></ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="TODO:Name"/>
        <TextBlock Grid.Row="1" Text="{Binding DateTaken, StringFormat='{}Date taken: {0:dd.MM.yyyy, HH:mm:ss}'}"/>
        <TextBlock Grid.Row="2" Text="{Binding DateAdded, StringFormat='{}Date added: {0:dd.MM.yyyy, HH:mm:ss}'}"/>

        <DockPanel Grid.Row="3" HorizontalAlignment="Stretch" >
            <StackPanel DockPanel.Dock="Left" Orientation="Horizontal">
                <ToggleButton IsChecked="{Binding Favorite, Mode=TwoWay}">
                    <ToggleButton.Style>
                        <Style TargetType="ToggleButton" BasedOn="{StaticResource IconToggleButton}">
                            <Style.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter Property="Content">
                                        
                                    </Setter>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ToggleButton.Style>
                    
                    
                    <Image Stretch="None">
                        <Image.Style>
                            <Style TargetType="Image">
                                <Style.Triggers>
                                    
                                    
                                    <DataTrigger Binding="{Binding Favorite}" Value="true">
                                        <Setter Property="Source" Value="/Resources/Icons/star.png"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Favorite}" Value="false">
                                        <Setter Property="Source" Value="/Resources/Icons/star-empty.png"/>
                                    </DataTrigger> 
                                    
                                </Style.Triggers>
                            </Style>
                        </Image.Style>
                    </Image>
                </ToggleButton>
                <ToggleButton Style="{StaticResource IconToggleButton}" IsChecked="{Binding Private, Mode=TwoWay}">
                    <Image Stretch="None">
                        <Image.Style>
                            <Style TargetType="Image">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Private}" Value="true">
                                        <Setter Property="Source" Value="/Resources/Icons/lock.png"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Private}" Value="false">
                                        <Setter Property="Source" Value="/Resources/Icons/lock_grey.png"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Image.Style>
                    </Image>
                </ToggleButton>
            </StackPanel>
            
        </DockPanel>
        <Button Grid.Row="4" HorizontalAlignment="Left" Style="{StaticResource IconButton}" Command="{Binding CopyTagsFromPreviousCommand}">
            <Image Source="/Resources/Icons/arrow-repeat.png" Stretch="None"/>
        </Button>
        <tags:TagsView Grid.Row="5" DataContext="{Binding TagsViewModel}"/>
    </Grid>
</UserControl>
