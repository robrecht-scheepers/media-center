<UserControl x:Class="MediaCenter.Media.ImageItemContentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MediaCenter.Media"
             xmlns:controls="clr-namespace:MediaCenter.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance Type=local:ImageItemViewModel}">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <controls:ZoomImage Grid.Row="0" Grid.RowSpan="2" ImageContent="{Binding Path=MediaItem.Content}" />
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
            <controls:ImageButton ImageSource="/Resources/Icons/arrow-circle-315-rev.png" Width="32" Height="32" Command="{Binding RotateCounterclockwiseCommand}"/>
            <controls:ImageButton ImageSource="/Resources/Icons/arrow-circle-315.png" Width="32" Height="32" Command="{Binding RotateClockwiseCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
