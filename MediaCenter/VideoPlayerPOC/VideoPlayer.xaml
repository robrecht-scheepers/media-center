<UserControl x:Class="VideoPlayerPOC.VideoPlayer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VideoPlayerPOC"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance d:Type=local:VideoPLayerViewModel}">

    <i:Interaction.Behaviors>
        <local:VideoControlBehavior RequestedPlayerState="{Binding Path=PlayState}" VideoFilePath="{Binding VideoFilePath}" />
    </i:Interaction.Behaviors>

    <Grid Name="Grid1">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <MediaElement Grid.Row="0"  Name="MediaElement" LoadedBehavior="Manual"/>
        <StackPanel Grid.Row="1"  Orientation="Horizontal">
            <Button Command="{Binding LoadCommand}">Load</Button>
            <Button Command="{Binding PlayCommand}">Play</Button>
            <TextBlock Text="{Binding PlayState}"/>
        </StackPanel>
    </Grid>
</UserControl>
